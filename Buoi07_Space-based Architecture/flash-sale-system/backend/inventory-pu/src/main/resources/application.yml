server:
  port: 8084

spring:
  application:
    name: inventory-pu
  config:
    import: optional:file:../../.env[.properties]
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000

logging:
  level:
    com.flashsale.inventory: INFO
