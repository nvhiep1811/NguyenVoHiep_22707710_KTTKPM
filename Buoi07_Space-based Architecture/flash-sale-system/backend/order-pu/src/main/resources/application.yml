server:
  port: 8083

spring:
  application:
    name: order-pu
  config:
    import: optional:file:../../.env[.properties]
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000

inventory:
  service:
    url: http://localhost:8084

logging:
  level:
    com.flashsale.order: INFO
