server:
  port: 8082

spring:
  application:
    name: cart-pu
  config:
    import: optional:file:../../.env[.properties]
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000

logging:
  level:
    com.flashsale.cart: INFO
