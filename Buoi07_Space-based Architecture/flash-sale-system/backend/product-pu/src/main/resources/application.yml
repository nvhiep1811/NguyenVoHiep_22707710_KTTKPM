server:
  port: 8081

spring:
  application:
    name: product-pu
  config:
    import: optional:file:../../.env[.properties]
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000

logging:
  level:
    com.flashsale.product: INFO
